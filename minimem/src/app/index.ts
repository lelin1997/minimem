// ============================================================
// MiniMem — src/app/index.ts (A01: app 层 barrel)
// ============================================================
// 应用编排层入口
// 依赖规则：可依赖所有层；不可被任何层依赖

export { startMinimem } from './main.js';
